// SDIZO.cpp : Defines the entry point for the console application.
//

#include "stdafx.h"
#include "Table.h"
#include "TWList.h"
#include <string>
#include <conio.h>
#include <stdio.h>
#include <fstream>
#include "SDIZO.h"





int main()
{
	TWList tWList;
	
	/*tWList.addN(2);
	cout << tWList.current->value;
	tWList.addN(4);
	cout << tWList.current->value;
	tWList.addN(6);
	cout << tWList.current->value;*/
	
	/*
	TWList tWList;

	RandData randData;
	randData.zapis(10);
	std::ifstream plik;
	plik.open("dane.txt");
	int zmienna;
	while (plik >> zmienna)
	{
		tWList.addN(zmienna);
	}
	
	tWList.printlallall();
	tWList.addN(69);
	tWList.printall();
	tWList.addM(77, 4);
	cout << tWList.addCount(88, 2);
	*/

		

	



    return 0;
}

